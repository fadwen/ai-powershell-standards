---
paths:
  - "**/Tests/**/*.ps1"
  - "**/*.Tests.ps1"
---

@../../../.github/instructions/pester.instructions.md

Source of this rule: `.github/instructions/pester.instructions.md` (shared with GitHub Copilot, which
applies it through its `applyTo` glob). Relative links inside it resolve from `.github/instructions/`.
