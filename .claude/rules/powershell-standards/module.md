---
paths:
  - "**/*.psm1"
  - "**/*.psd1"
  - "**/Public/*.ps1"
  - "**/Private/*.ps1"
---

@../../../.github/instructions/module.instructions.md

Source of this rule: `.github/instructions/module.instructions.md` (shared with GitHub Copilot, which
applies it through its `applyTo` glob). Relative links inside it resolve from `.github/instructions/`.
