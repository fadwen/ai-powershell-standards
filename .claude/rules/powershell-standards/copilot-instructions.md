@../../../.github/copilot-instructions.md

Source of this rule: `.github/copilot-instructions.md`, the always-on standards GitHub Copilot applies to
every file. This rule has no `paths`, so Claude Code loads it at launch the same way. Relative links
inside it resolve from `.github/`.
